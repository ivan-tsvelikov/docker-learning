server:
  port: 5000
spring:
  redis:
    port: 6379
    host: 127.0.0.1
  jpa:
    database: postgresql
    show-sql: true
    open-in-view: false
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: password
    driverClassName: org.postgresql.Driver
  flyway:
    url: jdbc:postgresql://localhost:5432/postgres
    user: postgres
    password: password